# activates vi mode for zsh
bindkey -v 

# binds backspace and delete key to "normal" function
bindkey "^?" backward-delete-char
bindkey "\e[3~" delete-char

# only for debugging
# echo "...zsh_keybindings loaded"
